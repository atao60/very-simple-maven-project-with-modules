very-simple-maven-project-with-modules
======

Install
------

With:
    
      cd <the aggregator project path>
      mvn install
      
the jar and war files will be installed in the local m2 repository.      